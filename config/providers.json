{
  "schema_version": "0.2",
  "real_execution": "bounded_week_one_only",
  "allowed_runtime_providers": [
    "mock",
    "openrouter via scripts/free_provider.py"
  ],
  "denied_providers": [
    "groq"
  ],
  "deny_includes_aggregator_upstreams": true,
  "default_real_model": null,
  "policy_question": "Q-OPS-PROVIDER-POLICY",
  "policy_resolution": "Later user clarification preserved in PR 5: explicit role-fit free models may be tested and used; actual identity and outcome are retained. Zero spend and no Groq.",
  "enabled_route": {
    "model": "nvidia/nemotron-3-super-120b-a12b:free",
    "upstream": "nvidia",
    "price_ceiling": 0,
    "prerequisites": [
      "live zero-price model-specific catalog",
      "read-only confirmation of no active applicable BYOK",
      "durable request checkpoint",
      "bounded budget and time window"
    ],
    "verification": "per-generation identity, cost and BYOK receipt"
  },
  "generic_worker": "scripts/worker.py remains mock or deferred Gemini; Week One uses its own reviewed adapter.",
  "gemini": "Deferred; retain prompts. No automatic downgrade or fallback."
}
