# Work integration — 23 September 2026

Chat contribution: PR #4, head `25cee5fa5184ebdd5e3b66e4018656ed8e9e6e8f`. Research base: main `51e0969691b3637f7cac9efab38b4596043658b4`, RC-004 committed. User asked Work to make the best useful progress without another decision while they were tired. Original Chat files are preserved; this record identifies what actually became operational.

## Implemented and exercised

1. Shared question CLI and atomic JSON store. Registered all 30 existing research questions with original references; added one nonblocking model-policy question/ticket. IDs persist across roles and later cycles. No new empirical answers are claimed.
2. Single-checkout concurrent claims; duplicate normalized question detection; explicit relationships; evidence epochs; immutable attempt requests and final outcomes; attributed answers; escalation-only tickets. Chat answers can resolve the same question and stop redundant worker computation.
3. Worker v0.2 saves exact prompts and provenance before execution. Mock output remains mock. Real-provider requests record `policy_blocked` without reading a key or making a call. Removed the unverified Flash default and the active network adapter pending a reviewed replacement. Checkpoints are immutable and default to operations, protecting closed research inboxes. Previous adapter code remains in Git history.
4. Public RSS/Atom scout, independent of AI availability. HTTPS/approved-host inputs, no redirect following, bounded reads, restricted XML parsing and metadata-only retention. It preserves prior successes and an attempt history; repeated items are not new discoveries.
5. Both EIA feeds retrieved successfully after adding their advertised `text/xml` format to the request. The first HTTP 406 results and successful retries are both retained. Initial discovery: one fuel-update item and 18 Today in Energy items. These are unreviewed leads, not 19 new systemic stresses or source-ledger admissions. Feed publication dates do not establish event or observation dates.
6. Oakland Commons Mapper definition, commons capability record and editable tri-fold source integrated into their durable locations. Participation language remains qualitative; no named attendance tracking or numeric conversion. Existing users' reserve floors and reciprocal needs remain explicit. Added the proposed founding-song links to README without lyrics.
7. Worker Actions workflow retains request/attempt/checkpoint artifacts on ordinary failure and uses no model secret. No scheduler was added. A platform terminating a runner before artifact upload remains a recovery limitation; hard-kill durability is not claimed across GitHub runners.

## Validation

- 26 tests passed on Linux: nine existing cycle-integrity tests, seven question/coordination tests, three source-scout tests and seven worker tests.
- Tests include concurrent claims, interruption before a result, immutable failures, duplicate answer/ticket submissions, stale answers after reopening, mock nonpromotion, exact pre-call retention, real-provider blocking, safe context paths, immutable checkpoint exports, repeated-feed deduplication, XML/URL rejection and retention after source failure.
- Canonical validation passed: 52 signals, 26 conditional links, 109 sources and all ten sections present. All ten views reproduced without differences.
- Canonical state, committed cycles, all prior research inboxes/reports, generated views, manifest and license were compared against main and remain unchanged.
- Two actual public source retrievals succeeded; no actual model call, private account read, outbound email or partner commitment occurred.
- This is coordinator implementation and test verification, not an independent code or evidence audit. Windows behavior was not exercised. Tests do not establish performance under multi-host/distributed contention.

## Deliberately unresolved

`Q-OPS-PROVIDER-POLICY` preserves a genuine instruction conflict: the earlier direct best-Gemini-only direction versus Chat's reported newer permission for evaluated free role-fit backups. No immediate answer is required from the user. Provider-neutral development and public research can continue. Groq remains excluded, including hidden aggregator selection. Candidate IDs, quotas and provider reset rules in Chat's proposal need official current verification before use.

The live multi-provider ring, learned activity governor, provider benchmarks, recursive specialist spawning, mail/Discord ingestion and Vercel portal remain proposed. This turn did not silently deploy any of them. The presence governor would need an explicit approved signal source and scheduling scope; an external worker cannot see Chat activity just because a document describes it.

Git preserves shared records only after publication. A local lock does not arbitrate remote workers: Actions artifacts and Chat contributions need coordinator admission. The prototype Chat schemas are preserved as design input; `operations/questions.json` currently stores attempts/answers/tickets nested per question and is governed by the tested runtime, not by a claim of conformance to those older standalone schema drafts.

## Efficient next handoff

Chat Aiden: inspect `operations/questions.json`, pick one bounded unanswered research question or prepare one provider adapter/evaluation packet. Return its Question ID and epoch, exact source dates, uncertainty and counterevidence. Avoid repeating the broad reconnaissance. Stage work in a unique directory/PR as before.

Work Aiden: resolve model policy when the user naturally returns to it; verify one eligible provider; integrate a bounded adapter and retry/reset records before enabling live calls. Fresh empirical findings belong in the next open research cycle, not a rewrite of RC-004. The EIA discovery records can guide that pass, but their links must be opened and relevant values verified.

The pending policy decision does not require the user to research models, maintain ledgers, supply keys in chat or coordinate agents manually.
